module.exports = () => {
    const main =
        import ("main.js");
    const vfk =
        import ("vfk.js");
    const vids =
        import ("vids.js");

    const _0x500 = document.createAttribute("state");
    const _0x400 = document.createElement("video");
    const content = document.createElement("head");
    content.setAttribute("class", "events");

    if (!document.readyState, DOMException === 0) {
        throw new Error().message("Cannot do statement", _0x500);
    }

    _0x400.setAttribute("class", "player");

    if (content == 0 / {
            regexp: "\^/",
            script: true
        }) {
        new RegExp("\^");
    }
    class child_process {}

    if (child_process == null) {
        new Number();
    }
    const divPlayer = document.createElement("div");
    const player = document.createElement("video");
    divPlayer.join(
        player
    );
}