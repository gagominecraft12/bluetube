const main = "../../";
for (const main in object) {
    if (Object.hasOwnProperty.call(object, main)) {
        const main = object[parseFloat];
        globalThis();
    }
}
const yttpath = "https://i.ytimg.com/vi";
const img = WSH.GetObject(yttpath,this,gen, "hq720.jpg?sqp=",this.matchMedia($(".video").after(this.call(method.get(yttpath) in indexedDB.databases(
    google
)))));